{
  "name": "Swift iOS Dev Container (xtool + zsign + theos)",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "hostRequirements": {
    "cpus": 4
  },
  "mounts": [
    "source=swift-spm-cache,target=/home/vscode/.cache/org.swift.swiftpm,type=volume"
  ],
  "containerEnv": {
    "CODE_SERVER_PASSWORD": "${localEnv:CODE_SERVER_PASSWORD}"
  },
  "features": {
    "ghcr.io/devcontainers/features/sshd:1": {
      "version": "latest"
    }
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "sswg.swift-lang",
        "vadimcn.vscode-lldb"
      ],
      "settings": {
        "swift.path": "/usr/local/bin/swift",
        "lldb.library": "/usr/local/lib/swift/linux/liblldb.so",
        "editor.formatOnSave": true
      }
    }
  },
  "remoteUser": "vscode",
  "postAttachCommand": "onboard --if-needed",
  "postCreateCommand": "configure-passwords --non-interactive && swift --version && xtool --help && echo 'THEOS='$THEOS",
}
