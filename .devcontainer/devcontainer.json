{
  "name": "Swift iOS Dev Container (xtool + zsign + theos)",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "sswg.swift-lang",
        "vadimcn.vscode-lldb"
      ],
      "settings": {
        "swift.path": "/usr/local/bin/swift",
        "lldb.library": "/usr/local/lib/swift/linux/liblldb.so"
      }
    }
  },
  "remoteUser": "vscode",
  "features": {},
  "postCreateCommand": "swift --version && xtool --help && zsign -h && echo 'THEOS='$THEOS"
}
